<!--
Auto-generated by Pillaro.Dataverse.PluginFramework NuGet package.
Do not edit this file. It may be regenerated on build/package update.
-->
# 1Deployment Tools

This folder contains lightweight wrapper scripts that invoke the Pillaro Dataverse CLI from the NuGet package. Use the PowerShell wrapper in CI; the .bat wrapper is convenient for local Windows runs.

## Quick start
From project root (Windows):

    .\Tools\Deployment\DeployPlugins.bat

> [!IMPORTANT]
> Do not store connection strings or secrets in committed files.

## Files
| File | Owner | Update behavior |
|---|---|---|
| PillaroSettings.json | User | Create only if missing. Never overwrite. |
| Tools/EarlyBound/EarlyBoundSettings.json | User | Create only if missing. Never overwrite. |
| Tools/Deployment/DeployPlugins.bat | Package | Regenerate on build when content changed. |
| Tools/Deployment/DeployPlugins.ps1 | Package | Regenerate on build when content changed. |
| Tools/Deployment/README.md | Package | Regenerate on build when content changed. |
| Tools/EarlyBound/GenerateEarlyBound.bat | Package | Regenerate on build when content changed. |
| Tools/EarlyBound/README.md | Package | Regenerate on build when content changed. |

## Configuration
PillaroSettings.json lives in the project root and controls deployment profiles. It is user-owned and never overwritten by the package.

## Local environment
Set the Dataverse connection string in the user environment (PowerShell):

    [Environment]::SetEnvironmentVariable('DV_CONN', 'AuthType=ClientSecret;Url=https://your-org.crm4.dynamics.com;ClientId=your-client-id;ClientSecret=your-client-secret', 'User')

Restart Visual Studio, PowerShell or your terminal after setting the variable.

Temporary session-only setup:

    $env:DV_CONN = 'AuthType=ClientSecret;Url=https://your-org.crm4.dynamics.com;ClientId=your-client-id;ClientSecret=your-client-secret'

Safe verification:

    [Environment]::GetEnvironmentVariable('DV_CONN', 'User') -ne $null

## Azure DevOps
Example pipeline step:

~~~yaml
- powershell: |
    .\Tools\Deployment\DeployPlugins.ps1 -Profile ci
  displayName: Deploy Dataverse plugins
  env:
    DV_CONN: $(DataverseConnectionString)
~~~

> [!TIP]
> If Visual Studio does not show generated files immediately, reload the project or enable Show All Files.

Do not pass connection strings as command-line arguments. CI profile may use `${BUILD_BINARIESDIRECTORY}` in PillaroSettings.json.
